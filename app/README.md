## Simple example to use vllm
To launch, use the command:
Install <a href="https://docs.astral.sh/uv">uv</a> (if not already)
<code>uv pip install -r requirements.txt</code>
<code>docker-compose up</code>